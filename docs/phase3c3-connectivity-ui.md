# Connectivity UI

## Status: BLOCKED

The transport layer exposes lifecycle states, but no production Compose state model/screen currently displays independent internet, Wi-Fi Direct, Bluetooth, active transport, connected peer, and pending queue state. No demo values were added.
